# Agent Tests Docs Index

This directory contains memory-system testing documents with different purposes.

## Files

- `memory_system_test_suite.md`
  - Current detailed test-case suite (`TEST-001` to `TEST-034`).
  - Use this as the primary checklist and reference for case-level validation.

- `memory_system_automation_plan_initial.md`
  - Preserved original automation-plan draft (initial version).
  - Keep for historical reference and comparison.

- `memory_system_automation_plan.md`
  - Refined automation-plan notes with implementation-oriented corrections.
  - Use when translating the initial plan into CI-ready contract tests.

## Suggested Usage

1. Start from `memory_system_test_suite.md` for concrete coverage targets.
2. Compare with `memory_system_automation_plan_initial.md` when tracing original intent.
3. Use `memory_system_automation_plan.md` for practical rollout details.
