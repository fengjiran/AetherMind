---
scope: module
module: [MODULE_NAME]
parent: none
depends_on: []
adr_refs: []
last_verified: YYYY-MM-DD
owner: [agent|team]
status: active
---

# [MODULE_NAME] 主模块记忆模板

> 保存路径：`docs/agent/memory/modules/[MODULE_NAME]/module.md`
> 用途：记录主模块的长期稳定信息。只写已验证事实；缺失信息写 `无` 或 `未涉及`。

## 模块范围
### 职责
- 负责：[主模块直接负责的能力]
- 不负责：[明确不属于本模块的能力]

### 边界
- 上游输入：[调用方 / 输入对象 / 前置条件]
- 下游输出：[被调用方 / 输出对象 / 可见副作用]
- 不直接管理：[跨模块对象、资源或状态]

### 子模块划分
- `[SUBMODULE_NAME_A]`：[职责一句话]；文件：`docs/agent/memory/modules/[MODULE_NAME]/submodules/[SUBMODULE_NAME_A].md`
- `[SUBMODULE_NAME_B]`：[职责一句话]；文件：`docs/agent/memory/modules/[MODULE_NAME]/submodules/[SUBMODULE_NAME_B].md`
- 未涉及：无

## 已确认事实
- 已验证约束：[来自代码、测试、用户指令或已确认设计的稳定结论]
- 已验证限制：[当前实现必须遵守的边界条件]
- ADR 关联：[如有，引用 `./adrs/ADR-XXX.md`]
- 非阻塞注意事项：[已确认存在，但当前不阻塞推进]
- 未涉及：无

## 核心抽象
### 关键抽象
- `[TypeOrConceptA]`：[角色、状态或职责]
- `[TypeOrConceptB]`：[角色、状态或职责]

### 数据流
- 输入：[从哪里进入]
- 转换：[关键处理阶段]
- 输出：[流向哪里]
- 未涉及：无

## 对外接口
- 头文件 / API：[例如 `include/...` 中暴露的类、函数、枚举]
- 入口函数：[签名、调用时机、前置条件]
- 错误语义：[返回值、异常约定、失败处理]
- 未涉及：无

## 不变量
- [必须始终成立的条件 1]
- [必须始终成立的条件 2]
- 未涉及：无

## 所有权与生命周期
- 所有者：[哪个对象拥有哪个资源]
- 借用关系：[谁可以短期访问，谁不能长期保存]
- 销毁时机：[资源何时释放，释放前需满足什么条件]
- 未涉及：无

## 并发约束
- 线程角色：[哪些线程读 / 写 / 调用]
- 同步要求：[锁、原子、阶段屏障或线程亲和性]
- 禁止事项：[哪些对象不能跨线程共享或重入]
- 未涉及：无

## 性能约束
- 热路径：[必须重点控制开销的路径]
- 时间复杂度 / 常数项要求：[例如避免隐藏 O(N^2)]
- 内存约束：[分配次数、缓存命中、对象布局]
- 未涉及：无

## 已否决方案
- 方案：[已明确放弃的设计]
  - 原因：[为什么不采用]
- 无

## 未决问题
- [尚未定案且会影响后续实现的问题]
- 无

## 待办事项
- [ ] [下一步需要完成的具体工作]
- [ ] [需要补齐的测试、文档或验证]
- 无
